{
  "evals": [
    {
      "id": "private-gitee-default-branch",
      "prompt": "Monitor a private Gitee repository's default branch and submit updates to the LazyCat official store.",
      "mustInclude": ["version: 2", "auth_ref", "branch: auto", "LZC_API_TOKEN"],
      "mustNotInclude": ["SSH private key value", "publish-private"]
    },
    {
      "id": "custom-oci-image",
      "prompt": "Monitor an upstream OCI image, add features with a Dockerfile, and publish the LPK.",
      "mustInclude": ["mode: dockerfile", "SOURCE_IMAGE", "output_image", "official"],
      "mustNotInclude": ["latest only", "private store"]
    }
  ]
}
